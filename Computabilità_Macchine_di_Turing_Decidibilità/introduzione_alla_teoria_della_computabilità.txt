TESI DI CHURCH TURING -> limite massimo 
    -> hence, un problema non risolubile da una MdT è IRRISOLUBILE (da una macchina)

Irrisolubilità non significa che ho scritto dei bug, significache non riesce a risolvere il problema in tempo finito (loop infinito).
NB: NON c'è modo di distinguere una macchina inlooppata da una macchina che ci sta mettendo tanto tempo.
NB: a volte l'irrisolubilità di un problema si manifesta in base ai dati d'ingresso. Alcuni problemi vengono etichettati come irrisolubili anche se si riescono a risolvere
in 99 casi su 100.

Quali sono i problemi irrisolubili? possiamo evitarli?

PROBLEMI E FUNZIONI
cose diverse!

risolvere un problema significa avere le risposte ad ogni ingresso.

funzione caratteristica di un problema: ...

che cos'è una funzione computabile? -> equivale a quale funzione può essere calcolata da una macchina di Turing (in un numero finito di passi)?

una funzione NON computabile è una funzione che, anche in un solo caso, va in loop.

qualunque informazione può essere rappresentata con un naturale! Per questo motivo è possibile limatarsi alla funzioni da N a N nel considerare le funzioni definibili.

le funzioni da N a N sono non numerabile. le macchine di turing invece sono numerabili.
Conclusione: la grande maggioranza delle funzioni definibili non è computabile. COSA?!??!?!?

In realta, me ne sbatto della maggior parte delle funzioni da N a N (godo!). I due insiemi hanno quindi la stessa cardinalità(godo!!), ma coincidono? Basta trovare un 
problema definibile ma non computabile.

PROBLEMA DELL'HALT DELLA MACCHINA DI TURING
...

Il prossimo passo è indagare la decidibilità dei problemi che ci interessano. In particolare la decidibilità di un insieme.

idea: stringa appartiene ad un insieme valido (analisi sintattica di un compilatore)

SEMI-DECIDIBILITà e DECIDIBILITà

esempio: un numero è pari se PRIMA O POI quel numero verrà prodotto dalla macchina di Turing. Un numero NON è pari se la macchina di Turing non lo produrrà mai.

conclusione: il fatto che un insieme sia generabile, NON basta per dire se un elemento appartiene o meno all'insieme. (gli elementi fuori dall'insieme causano un loop, 
per capire se l'elemento appartiene devo controllare TUTTI gli elementi dell'insieme, possibilmente infiniti)

INSIEMI DECIDIBILI
....
una macchina di Turing sa sempre rispondere alla domanda dell'appartenenza

TEOREMI SU INSIEMI DECIDIBILI